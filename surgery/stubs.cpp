#include <cstring>


int readline(int fd, char* buffer) {
    strcpy(buffer, "a read line");
    return 0;
}


void fetchMarketingSlogans(char const* searchString, char* output) {
    strcpy(output, "Buy from ProAgile!");
}
