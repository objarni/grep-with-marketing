#include <stdlib.h>
#include <stdio.h>
#include "cloudservice.h"


void fetchMarketingSlogans(const char *searchTerm, char *slogans) {
    printf("You cannot call this from the unit test.");
    exit(1);
}
